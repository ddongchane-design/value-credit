# 배포 안내 — Cloudflare Pages

GitHub에 push하면 Cloudflare가 자동으로 배포합니다. 로컬에 Node·빌드 도구는 필요 없습니다 —
functions/ 컴파일은 Cloudflare 빌드 서버가 대신 해 줍니다.

## 폴더 구성

```
site/
  functions/
    _middleware.js             비밀번호 게이트 (전 페이지에 적용)
  public/                      ← Build output directory
    index.html                 메인 홈페이지          →  /
    valuation/index.html       채무조정채권 평가판     →  /valuation/
    dd-spec/index.html         DD 컬럼 표준 명세      →  /dd-spec/
    _headers                   보안 헤더 · 검색 차단
    robots.txt                 크롤러 차단
```

`functions/` 는 출력 디렉터리(`public/`) **밖**에 있어야 합니다. 안에 두면 Worker로
컴파일되지 않고 정적 파일로 업로드되어, 게이트가 동작하지 않으면서 소스만 노출됩니다.

`public/valuation/index.html`과 `public/dd-spec/index.html`은 원본을 **그대로 복사**한 것입니다.
평가판이나 명세를 고치면 그 파일을 다시 이 자리에 복사하고 재배포하면 됩니다.

## 1. 배포 — GitHub 연동

저장소: <https://github.com/ddongchane-design/value-credit> (Private)

1. <https://dash.cloudflare.com> → **Workers & Pages** → **Create** → **Pages** 탭
2. **Connect to Git** → GitHub 인증 → Private 저장소라 **Only select repositories** 에서
   `value-credit` 를 명시적으로 선택
3. 빌드 설정:

   | 항목 | 값 |
   |---|---|
   | Framework preset | `None` |
   | Build command | **비워둠** |
   | **Build output directory** | **`public`** |
   | Root directory | **비워둠** |

4. **Save and Deploy**

> 이 시점에는 **503 오류**가 뜹니다. 비밀번호를 안 넣었기 때문이고 정상입니다.
> 비밀번호 없이 열린 채로 방치되지 않도록 일부러 그렇게 막아뒀습니다.

## 2. 비밀번호 설정 (필수)

1. 프로젝트 → **Settings** → **Variables and Secrets** → **Add**
2. 아래를 추가합니다. **반드시 Type을 `Secret`(암호화)으로** 지정하세요 —
   `Text`로 넣으면 대시보드에 비밀번호가 평문으로 보입니다.

   | 이름 | 값 | 필수 |
   |---|---|---|
   | `SITE_PASSWORD` | 원하는 비밀번호 | 필수 |
   | `SITE_USER` | 원하는 아이디 | 선택 (기본 `welrix`) |

3. **저장만으로는 적용되지 않습니다.** **Deployments** 탭 → 최신 배포의 `⋯` → **Retry deployment**
   (이 한 번만 수동이고, 이후 `git push` 배포에는 자동으로 붙습니다)

이제 접속하면 브라우저가 아이디·비밀번호를 묻습니다.

## 3. 커스텀 도메인 (선택)

프로젝트 → **Custom domains** → **Set up a domain**.
도메인이 Cloudflare에 등록돼 있으면 DNS가 자동으로 잡히고, 인증서도 자동 발급됩니다.

## 갱신할 때

```bash
cd "c:/Users/ddong/OneDrive/Desktop/회사업무/채권평가/site"
git add -A
git commit -m "무엇을 바꿨는지"
git push
```

push하면 1분 안에 자동 반영됩니다. 브랜치를 파면 프리뷰 URL이 따로 생기고,
대시보드 **Deployments** 에서 이전 배포로 원클릭 롤백됩니다.
비밀번호 환경변수는 배포와 무관하게 유지됩니다.

## 알아둘 것

- **DD 엑셀은 서버로 가지 않습니다.** 평가판은 전부 브라우저 안에서 돌아가고,
  Cloudflare는 HTML 파일만 내려줍니다. 업로드한 DD가 Cloudflare에 남는 일은 없습니다.
- **Basic Auth는 브라우저가 비밀번호를 기억합니다.** 공용 PC에서 열었다면 창을 완전히 닫아야 합니다.
- **비밀번호는 링크와 함께 전달되는 순간 통제를 벗어납니다.** 공유 범위를 좁게 유지하세요.
- 무료 플랜 한도는 이 용도로는 넉넉합니다 (요청 10만/일, 정적 파일 무제한).
